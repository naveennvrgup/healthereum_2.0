import React from "react";
import { Text, StyleSheet } from "react-native";

const Visit = () => {
  return <Text style={styles.text}>Visit</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30
  }
});

export default Visit;
